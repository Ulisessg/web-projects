import styled from 'styled-components';

export const SkipLinkContainer = styled.section`
  background-color: ${(props) => props.theme.backgroundLight};
`;

export const LinkContainer = styled.span`
  display: inline;
`;
