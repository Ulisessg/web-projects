import styled, { createGlobalStyle } from 'styled-components';

export const GistsCoverImage = styled.div`
width: 100%;
display: flex;
justify-content: center
`;

export const GistsClassesStyles = createGlobalStyle`
  h1, h2, h3, h4 {
    text-align: center;
    margin-top: 20px;
    margin-bottom: 15px;
  }
`;
