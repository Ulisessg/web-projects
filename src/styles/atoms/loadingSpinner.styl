@import '../variables.styl'

.loading-spinner
  margin 0 auto
  width: 45px
  height: 45px
  border-radius: 50%
  border: 4px solid rgba(0,0,0,0.1)
  border-left: 4px solid background-dark
  animation: spin 0.8s linear infinite


@keyframes spin {
  100% {
    transform: rotate(360deg)
  }
}